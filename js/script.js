// JQuery Code //
$(document).ready(function () {
    // SuperSlides
    $('#slides').superslides({
        animation: 'fade',
        play: 5000,
        pagination: false
    });

// JS Code //

// Typed JS
var typed = new Typed(".typed", {
    strings: ["Web Developer.", "Full Stack.", "Always Learning."],
    typeSpeed: 70,
    loop: true,
    startDelay: 1000,
    showCursor: false
});








});